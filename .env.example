# Local dev (Phase 1): SQLite file path, no Docker/Supabase needed yet.
SQLITE_PATH=local.db

# Phase 2+: real Postgres connection (Supabase or Neon). Switch drizzle.config.ts
# and src/db/index.ts over to schema.ts (Postgres dialect) when this is set.
# DATABASE_URL=
